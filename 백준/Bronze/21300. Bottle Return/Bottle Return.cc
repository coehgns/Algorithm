#include <iostream>
using namespace std;

int main() {
  int b, b2, w, cd, cw, w2;

  cin >> b >> b2 >> w >> cd >> cw >> w2;
  cout << b * 5 + b2 * 5 + w * 5 + cd * 5 + cw * 5 + w2 * 5;

  return 0;
}