#include <bits/stdc++.h>
using namespace std;

int main() {
    int b, g;

    while(1) {
        cin >> b >> g;
        if(b == 0 && g == 0) break;
        cout << b + g << '\n';
    }

    return 0;
}